Название проекта: Telegram-бот для распознавания и перевода меню

Описание:
Разработка Telegram-бота на Python, который позволяет пользователю отправить фотографию печатного меню (на любом языке, кроме русского).
Бот распознаёт текст, переводит его на русский язык, классифицирует позиции на блюда и напитки. 
Для каждого блюда определяет состав, наличие молока и глютена. 
В ответ пользователь получает HTML-страницу с красиво оформленным переведённым меню.

Основные функции:
1. Получение фото меню от пользователя через Telegram.
2. Распознавание текста на фот.
3. Определение языка исходного текста (исключение — русский).
4. Перевод текста на русский язык.
5. Классификация элементов меню: блюда и напитки.
6. Для каждого блюда (только не напитки):
   - Генерация описания блюда на основе его полного наименования.
   - Генерация списка ингредиентов.
   - Генерация изображения блюда на основе его описания.
   - Определение наличия молока (да/нет/неизвестно).
   - Определение наличия глютена (да/нет/неизвестно).
7. Генерация HTML-документа с отформатированным, переведённым меню.
8. Отправка HTML-файла пользователю.

Технические детали:
- Язык: Python 3.11+
- Среда разработки: VS Code
- Менеджер пакетов: [uv](https://github.com/astral-sh/uv)
- Установка зависимостей: `uv add <package>`
- Запуск скриптов: `uv run python script.py`
- Использование возможностей модели gemini-2.0-flash для:
   - Перевода текста
   - Классификации позиций меню
   - Генерации состава и анализа на молоко/глютен
- Использование возможностей модели gemini-2.5-pro для:
   - Генерации изображения блюда
- Количество обращений к модели при работе с каждым блюдом должно быть равно единице
- Сторонние библиотеки используются только при необходимости (например Telegram API)

Минимальные зависимости:
- `aiogram` (взаимодействие с Telegram API)


Интерфейс:
- Пользователь отправляет фото меню в чат.
- Бот возвращает HTML-файл с красивым переводом, сгруппированным по категориям.

Валидация:
- Фото не должно быть на русском языке. Если русский язык обнаружен — вернуть сообщение об ошибке.
- При отсутствии текста на изображении — уведомить пользователя.
- В случае невозможности распознать блюдо или определить состав — использовать `none`.

Статус MVP:
✅ Получение фото  
✅ OCR  
✅ Перевод  
✅ Классификация  
✅ Генерация состава и анализ  
✅ Генерация изображения 
✅ HTML-ответ пользователю  

Дополнительно:
- HTML-страница должна быть легко читаема, с группировкой: закуски, основные блюда, напитки и т.д.
- Возможно использование emoji или иконок для визуального оформления (например, 🥛 для молока, 🌾 для глютена).
